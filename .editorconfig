# Seeded once by Bob (go-hygiene@2). Yours to own and extend;
# Bob never updates or overwrites it.
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = tab
indent_size = 4

[*.md]
trim_trailing_whitespace = false
